type Props = {
  sliderRef: React.MutableRefObject<HTMLDivElement | undefined>;
};

const VisitSummary = ({ sliderRef }: Props) => {
  return <div className="visit-summary-page slide">VisitSummary</div>;
};

export default VisitSummary;
